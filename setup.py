from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A binnary classifier (score) to help detecting BOT accounts on twitter, using only profile-based features',
    author='Bruno Vieira',
    license='MIT',
)
